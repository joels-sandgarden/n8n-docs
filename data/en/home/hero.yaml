# The wordmark SVG is disabled (empty string, not deleted, so the E25DX theme
# default doesn't merge back in) — the header logo already carries the name.
svg: ""

badge: Written by Doc Holiday

# The <span> gets the gradient text treatment (see hero.css).
title: n8n, explained at the <span>concept level</span>.

description: A field guide to the n8n codebase — how the execution engine actually behaves, where the canvas and the engine disagree, and where to look in the code. Written by exploring the source directly, page by page.

btn_primary:
  label: Read the guide
  url: docs/the-big-picture/

btn_secondary:
  label: n8n on GitHub
  url: https://github.com/n8n-io/n8n
